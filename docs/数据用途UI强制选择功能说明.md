# 数据用途UI强制选择功能说明

## 📋 功能概述

为确保智能数据用途识别系统能准确工作，我们在K线数据导入UI中添加了**强制性数据用途选择**功能。用户在创建或编辑任务时必须明确选择数据用途，系统会自动将用途标记追加到任务名称中。

## 🎯 设计目标

1. **用户友好**：通过下拉框强制用户明确数据用途，避免模糊场景
2. **自动化标记**：系统自动管理任务名中的用途标记，用户无需手动输入
3. **一致性保证**：确保`_infer_data_usage`的方法1（任务ID关键词识别）始终能准确工作
4. **编辑灵活性**：编辑任务时可以修改用途，系统自动更新标记

## 🔧 实现细节

### 1. UI组件添加

#### 位置
在"基本信息"分组中，任务描述之后、资产类型之前

#### 代码位置
`gui/widgets/enhanced_data_import_widget.py` 行1072-1088

```python
# 🎯 数据用途（新增）- 用于智能权重配置
self.data_usage_combo = QComboBox()
self.data_usage_combo.addItems([
    "通用场景",      # general - 默认
    "历史数据分析",  # historical
    "回测验证",      # backtest
    "实时行情",      # realtime
    "实盘交易"       # live_trading
])
self.data_usage_combo.setToolTip(
    "选择数据用途后，系统会自动调整质量评分权重：\n"
    "• 历史数据分析：注重准确性和完整性\n"
    "• 回测验证：注重一致性和准确性\n"
    "• 实时行情：提高及时性权重\n"
    "• 实盘交易：最高及时性和准确性权重"
)
basic_layout.addRow("🎯 数据用途:", self.data_usage_combo)
```

### 2. 数据转换方法

#### `_get_data_usage_value()` - 获取英文枚举值
**位置**：`gui/widgets/enhanced_data_import_widget.py` 行4885-4908

**功能**：将中文显示名称转换为英文枚举值

**映射关系**：
| 中文显示名称 | 英文枚举值 | 用途说明 |
|------------|-----------|---------|
| 通用场景 | general | 平衡配置，适用于一般场景 |
| 历史数据分析 | historical | 注重准确性和完整性 |
| 回测验证 | backtest | 注重一致性和准确性 |
| 实时行情 | realtime | 提高及时性权重 |
| 实盘交易 | live_trading | 最高及时性和准确性权重 |

#### `_get_data_usage_tag()` - 获取中文标记
**位置**：`gui/widgets/enhanced_data_import_widget.py` 行4910-4933

**功能**：提取简短的中文标记用于追加到任务名

**映射关系**：
| 中文显示名称 | 中文标记 |
|------------|---------|
| 通用场景 | [通用] |
| 历史数据分析 | [历史] |
| 回测验证 | [回测] |
| 实时行情 | [实时] |
| 实盘交易 | [实盘] |

### 3. 创建任务时的处理

#### 位置
`gui/widgets/enhanced_data_import_widget.py` 行2671-2686

#### 逻辑流程

```python
# 1. 获取基础任务名
base_task_name = self.task_name_edit.text().strip()

# 2. 获取用途标记
usage_tag = self._get_data_usage_tag()  # 如 "[回测]"

# 3. 移除旧标记（如果存在）
import re
base_task_name = re.sub(r'\[(通用|历史|回测|实时|实盘)\]$', '', base_task_name).strip()

# 4. 追加新标记
final_task_name = f"{base_task_name}{usage_tag}"

# 5. 保存配置
config = {
    'name': final_task_name,  # 如："导入任务_20251105[回测]"
    'data_usage': self._get_data_usage_value(),  # 如："backtest"
    # ... 其他配置
}
```

#### 示例
- 用户输入：`我的数据导入任务`
- 选择用途：`回测验证`
- 最终任务名：`我的数据导入任务[回测]`
- 数据用途字段：`backtest`

### 4. 编辑任务时的处理

#### 位置
`gui/widgets/enhanced_data_import_widget.py` 行3781-3821（UI）、行3929-3999（保存逻辑）

#### UI显示逻辑

```python
# 1. 从任务名中移除标记，显示基础名称
import re
base_name = re.sub(r'\[(通用|历史|回测|实时|实盘)\]$', '', task.name).strip()
name_edit.setText(base_name)

# 2. 从任务名中提取用途标记，设置下拉框
if '[回测]' in task.name:
    data_usage_edit_combo.setCurrentText("回测验证")
elif '[实时]' in task.name:
    data_usage_edit_combo.setCurrentText("实时行情")
# ... 其他判断
```

#### 保存逻辑

```python
def save_changes():
    # 1. 获取基础名称和新用途
    base_name = name_edit.text().strip()
    usage_display = data_usage_edit_combo.currentText()
    
    # 2. 获取用途标记和英文值
    usage_tag = tag_mapping.get(usage_display, "[通用]")
    data_usage_value = usage_mapping.get(usage_display, "general")
    
    # 3. 组合最终任务名
    final_task_name = f"{base_name}{usage_tag}"
    
    # 4. 更新配置
    self.config_manager.update_import_task(
        task_id,
        name=final_task_name,
        data_usage=data_usage_value,
        # ... 其他字段
    )
```

### 5. 任务名格式规范

#### 格式标准
```
基础任务名[用途标记]
```

#### 示例
```
✅ 正确格式：
- 导入任务_20251105[回测]
- A股日线数据[历史]
- 实时行情任务[实盘]
- 测试导入[通用]

❌ 错误格式（系统会自动修正）：
- 导入任务_20251105回测  → 自动变为：导入任务_20251105[回测]
- [回测]导入任务         → 标记应在末尾
- 导入任务[回测][实时]   → 只能有一个标记
```

## 🔗 与智能识别系统的集成

### 调用链

```
创建/编辑任务
    ↓
保存任务配置（包含name和data_usage字段）
    ↓
任务执行：_execute_task(task_config)
    ↓
数据验证：_validate_imported_data(task_id=task_config.task_id)
    ↓
智能识别：_infer_data_usage(data, task_id)
    ↓
方法1：检查task_id中的关键词 ✅ 
    - 检测到"[回测]" → 返回 "backtest"
    - 检测到"[实盘]" → 返回 "live_trading"
    ↓
质量评分：calculate_quality_score(data_usage="backtest")
    ↓
动态权重：_get_dynamic_weights("backtest")
    - completeness: 0.25
    - accuracy: 0.35
    - consistency: 0.30  ✅ 回测需要高一致性
    - timeliness: 0.10   ✅ 降低及时性权重
```

### 优势

1. **强制性保证**：用户必须选择用途，无法跳过
2. **自动化管理**：系统自动处理标记，避免手动错误
3. **优先级最高**：`_infer_data_usage`方法1（任务ID关键词）优先级最高，确保用户意图被准确识别
4. **回退机制**：即使标记丢失，方法2（数据新鲜度）和方法3（数据量）仍能提供合理推断

## 📊 使用场景

### 场景1：历史数据回测
- **选择**：回测验证
- **任务名**：`沪深300回测数据[回测]`
- **权重配置**：accuracy=0.35, consistency=0.30, timeliness=0.10
- **适用于**：量化策略回测、历史模拟交易

### 场景2：实盘交易
- **选择**：实盘交易
- **任务名**：`实时交易数据[实盘]`
- **权重配置**：accuracy=0.35, timeliness=0.35（最高）
- **适用于**：自动交易系统、实时监控

### 场景3：历史数据分析
- **选择**：历史数据分析
- **任务名**：`A股历史行情[历史]`
- **权重配置**：accuracy=0.40, completeness=0.30, timeliness=0.05（最低）
- **适用于**：数据研究、趋势分析、学习

### 场景4：实时行情监控
- **选择**：实时行情
- **任务名**：`盘中行情监控[实时]`
- **权重配置**：timeliness=0.30, accuracy=0.30
- **适用于**：盘中监控、快速决策

### 场景5：通用数据导入
- **选择**：通用场景
- **任务名**：`数据导入测试[通用]`
- **权重配置**：平衡配置，各维度均衡
- **适用于**：测试、不确定场景

## 🛠️ 技术要点

### 正则表达式处理
```python
# 移除旧标记
re.sub(r'\[(通用|历史|回测|实时|实盘)\]$', '', task_name)
```
- `\[` 和 `\]`：匹配方括号字面量
- `(通用|历史|回测|实时|实盘)`：匹配任一用途标记
- `$`：确保标记在字符串末尾

### 字典映射设计
所有映射使用统一的键（中文显示名称），确保：
- UI显示一致性
- 转换逻辑统一
- 易于维护扩展

### 容错处理
所有转换方法都有默认值和异常处理：
```python
usage_mapping.get(display_name, "general")  # 默认值
try:
    # 转换逻辑
except Exception as e:
    logger.error(...)
    return "general"  # 回退值
```

## 📝 未来扩展

### 可能的新用途
如需添加新的数据用途，需要修改以下位置：

1. **UI下拉框选项**（2处）
   - `gui/widgets/enhanced_data_import_widget.py` 行1074-1080
   - `gui/widgets/enhanced_data_import_widget.py` 行3792-3798

2. **映射字典**（6处）
   - `_get_data_usage_value()` 映射字典
   - `_get_data_usage_tag()` 映射字典
   - `create_new_task` 中的映射
   - `edit_task` 中的2个映射字典
   - 正则表达式模式

3. **权重配置**
   - `core/services/enhanced_data_manager.py` 的 `_get_dynamic_weights()`

### 示例：添加"机器学习训练"用途
```python
# 1. UI选项
self.data_usage_combo.addItems([
    # ... 现有选项
    "机器学习训练"  # 新增
])

# 2. 映射
usage_mapping = {
    # ... 现有映射
    "机器学习训练": "ml_training"  # 新增
}

tag_mapping = {
    # ... 现有映射
    "机器学习训练": "[ML训练]"  # 新增
}

# 3. 正则
re.sub(r'\[(通用|历史|回测|实时|实盘|ML训练)\]$', '', task_name)

# 4. 权重配置（在enhanced_data_manager.py）
'ml_training': {
    'completeness': 0.35,  # 机器学习需要完整数据
    'accuracy': 0.35,      # 准确性也很重要
    'consistency': 0.25,   # 一致性
    'timeliness': 0.05     # 及时性不重要
}
```

## ✅ 验证清单

- [x] UI下拉框正常显示
- [x] 创建任务时自动追加标记
- [x] 编辑任务时正确提取和显示用途
- [x] 保存时正确更新任务名和data_usage字段
- [x] 任务名格式统一（基础名[标记]）
- [x] 旧标记能正确移除
- [x] `_infer_data_usage`能正确识别标记
- [x] 质量评分权重正确应用
- [x] 日志记录完整详细

## 📖 相关文档

- [智能数据用途识别系统集成验证报告](./智能数据用途识别系统集成验证报告.md)
- [智能数据质量评估系统使用指南](./智能数据质量评估系统使用指南.md)

---

**最后更新**：2025-11-05
**修改文件**：`gui/widgets/enhanced_data_import_widget.py`

