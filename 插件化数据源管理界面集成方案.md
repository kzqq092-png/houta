# 插件化数据源管理界面集成方案

## 🎯 集成目标

基于现有FactorWeave-Quant 的插件管理系统，无缝集成新的插件化数据源，确保用户可以通过统一的管理界面控制和修改所有数据源插件的参数。

---

## 🔍 现有插件管理系统分析

### 现有插件管理能力

通过代码分析发现，FactorWeave-Quant 已具备完善的插件管理基础设施：

#### 1. 核心插件管理组件
```python
# core/plugin_config_manager.py - 插件配置管理器
class PluginConfigManager:
    - 支持YAML格式配置文件
    - 提供安全策略控制
    - 支持插件启用/禁用
    - 支持动态配置更新

# core/plugin_manager.py - 插件管理器  
class PluginManager:
    - 插件生命周期管理
    - 插件分类管理
    - 事件信号系统(plugin_loaded, plugin_enabled等)
    - 支持插件热加载
```

#### 2. 现有UI管理界面
```python
# gui/dialogs/plugin_manager_dialog.py - 主插件管理界面
class PluginManagerDialog:
    - 插件列表显示
    - 插件状态控制
    - 插件配置对话框
    - 插件监控功能

# gui/dialogs/sentiment_plugin_manager_dialog.py - 情绪插件管理
class SentimentPluginManagerDialog:
    - 专门的插件类型管理
    - 动态配置修改
    - 实时参数调整
```

#### 3. 配置管理能力
- ✅ 支持多种控件类型：QCheckBox, QSpinBox, QDoubleSpinBox, QComboBox, QLineEdit
- ✅ 支持配置验证和类型转换
- ✅ 支持配置热更新和保存
- ✅ 支持权限控制和安全策略

---

## 🔧 集成改造方案

### 方案一：扩展现有插件管理系统（推荐）

#### 1. 扩展PluginType枚举

```python
# core/plugin_manager.py
class PluginType(Enum):
    """插件类型枚举 - 扩展数据源类型"""
    INDICATOR = "indicator"          # 现有：技术指标插件
    SENTIMENT = "sentiment"          # 现有：情绪分析插件
    STRATEGY = "strategy"            # 现有：策略插件
    
    # 新增：数据源插件类型
    DATA_SOURCE = "data_source"      # 数据源插件总类
    STOCK_DATA = "stock_data"        # 股票数据源插件
    FUTURES_DATA = "futures_data"    # 期货数据源插件  
    CRYPTO_DATA = "crypto_data"      # 数字货币数据源插件
    FOREX_DATA = "forex_data"        # 外汇数据源插件
```

#### 2. 扩展PluginConfigManager

```python
# core/plugin_config_manager.py
class PluginConfigManager:
    def __init__(self):
        # ... 现有初始化代码 ...
        
        # 新增：数据源插件配置支持
        self.data_source_configs = {}
        self.data_source_routing_rules = {}
        
    def create_data_source_config(self, plugin_id: str, asset_type: str, 
                                  config_schema: Dict[str, Any]) -> PluginConfig:
        """创建数据源插件配置"""
        config = PluginConfig(
            name=plugin_id,
            version="1.0.0",
            plugin_type=PluginType.DATA_SOURCE,
            config_data={
                'asset_type': asset_type,
                'priority': 50,
                'enabled': True,
                'fallback_enabled': True,
                'rate_limit': 100,  # 每分钟请求限制
                'timeout': 30,      # 超时时间(秒)
                'retry_count': 3,   # 重试次数
                'cache_ttl': 300,   # 缓存TTL(秒)
                **config_schema     # 插件特定配置
            }
        )
        
        self.plugin_configs[plugin_id] = config
        self.save_plugin_config(plugin_id, config)
        return config
    
    def update_routing_priority(self, asset_type: str, 
                               plugin_priorities: List[Tuple[str, int]]) -> bool:
        """更新数据源路由优先级"""
        try:
            self.data_source_routing_rules[asset_type] = plugin_priorities
            self._save_routing_rules()
            return True
        except Exception as e:
            logger.error(f"更新路由规则失败: {e}")
            return False
```

#### 3. 创建数据源插件管理UI

```python
# gui/dialogs/data_source_plugin_manager_dialog.py
class DataSourcePluginManagerDialog(QDialog):
    """数据源插件管理对话框"""
    
    def __init__(self, plugin_manager: PluginManager, parent=None):
        super().__init__(parent)
        self.plugin_manager = plugin_manager
        self.config_manager = plugin_manager.config_manager
        
        self.setWindowTitle("数据源插件管理")
        self.setMinimumSize(1000, 700)
        
        self.init_ui()
        self.load_data_source_plugins()
    
    def init_ui(self):
        """初始化UI"""
        layout = QVBoxLayout(self)
        
        # 创建选项卡控件
        self.tab_widget = QTabWidget()
        
        # 1. 插件管理选项卡
        self.plugin_management_tab = self._create_plugin_management_tab()
        self.tab_widget.addTab(self.plugin_management_tab, "插件管理")
        
        # 2. 路由配置选项卡
        self.routing_config_tab = self._create_routing_config_tab()
        self.tab_widget.addTab(self.routing_config_tab, "路由配置")
        
        # 3. 性能监控选项卡
        self.monitoring_tab = self._create_monitoring_tab()
        self.tab_widget.addTab(self.monitoring_tab, "性能监控")
        
        # 4. 数据质量选项卡
        self.quality_tab = self._create_quality_tab()
        self.tab_widget.addTab(self.quality_tab, "数据质量")
        
        layout.addWidget(self.tab_widget)
        
        # 底部按钮栏
        button_layout = QHBoxLayout()
        
        # 刷新按钮
        refresh_btn = QPushButton("🔄 刷新")
        refresh_btn.clicked.connect(self.refresh_all_data)
        button_layout.addWidget(refresh_btn)
        
        # 导出配置按钮
        export_btn = QPushButton("📤 导出配置")
        export_btn.clicked.connect(self.export_config)
        button_layout.addWidget(export_btn)
        
        # 导入配置按钮
        import_btn = QPushButton("📥 导入配置")
        import_btn.clicked.connect(self.import_config)
        button_layout.addWidget(import_btn)
        
        button_layout.addStretch()
        
        # 关闭按钮
        close_btn = QPushButton("关闭")
        close_btn.clicked.connect(self.close)
        button_layout.addWidget(close_btn)
        
        layout.addLayout(button_layout)
    
    def _create_plugin_management_tab(self) -> QWidget:
        """创建插件管理选项卡"""
        widget = QWidget()
        layout = QHBoxLayout(widget)
        
        # 左侧：插件列表
        left_panel = QGroupBox("数据源插件列表")
        left_layout = QVBoxLayout(left_panel)
        
        # 资产类型过滤
        filter_layout = QHBoxLayout()
        filter_layout.addWidget(QLabel("资产类型:"))
        
        self.asset_type_filter = QComboBox()
        self.asset_type_filter.addItems([
            "全部", "股票", "期货", "数字货币", "外汇", "债券"
        ])
        self.asset_type_filter.currentTextChanged.connect(self.filter_plugins)
        filter_layout.addWidget(self.asset_type_filter)
        
        filter_layout.addStretch()
        left_layout.addLayout(filter_layout)
        
        # 插件列表表格
        self.plugin_table = QTableWidget()
        self.plugin_table.setColumnCount(6)
        self.plugin_table.setHorizontalHeaderLabels([
            "插件名称", "资产类型", "状态", "优先级", "健康状态", "操作"
        ])
        
        # 设置表格样式
        header = self.plugin_table.horizontalHeader()
        header.setStretchLastSection(True)
        header.setSectionResizeMode(0, QHeaderView.ResizeToContents)
        header.setSectionResizeMode(1, QHeaderView.ResizeToContents)
        header.setSectionResizeMode(2, QHeaderView.ResizeToContents)
        header.setSectionResizeMode(3, QHeaderView.ResizeToContents)
        header.setSectionResizeMode(4, QHeaderView.ResizeToContents)
        
        self.plugin_table.itemSelectionChanged.connect(self.on_plugin_selection_changed)
        left_layout.addWidget(self.plugin_table)
        
        layout.addWidget(left_panel, 1)
        
        # 右侧：插件配置面板
        right_panel = QGroupBox("插件配置")
        right_layout = QVBoxLayout(right_panel)
        
        # 配置选项卡
        self.config_tab_widget = QTabWidget()
        
        # 默认显示欢迎页面
        welcome_widget = QLabel("请选择左侧插件进行配置")
        welcome_widget.setAlignment(Qt.AlignCenter)
        welcome_widget.setStyleSheet("color: #666; font-size: 14px;")
        self.config_tab_widget.addTab(welcome_widget, "欢迎")
        
        right_layout.addWidget(self.config_tab_widget)
        layout.addWidget(right_panel, 1)
        
        return widget
    
    def _create_routing_config_tab(self) -> QWidget:
        """创建路由配置选项卡"""
        widget = QWidget()
        layout = QVBoxLayout(widget)
        
        # 路由策略选择
        strategy_group = QGroupBox("路由策略配置")
        strategy_layout = QGridLayout(strategy_group)
        
        # 全局路由策略
        strategy_layout.addWidget(QLabel("默认路由策略:"), 0, 0)
        self.default_routing_strategy = QComboBox()
        self.default_routing_strategy.addItems([
            "优先级路由", "负载均衡", "数据质量路由", "成本优化路由"
        ])
        strategy_layout.addWidget(self.default_routing_strategy, 0, 1)
        
        # 故障切换策略
        strategy_layout.addWidget(QLabel("故障切换策略:"), 1, 0)
        self.failover_strategy = QComboBox()
        self.failover_strategy.addItems([
            "立即切换", "延迟切换", "手动切换"
        ])
        strategy_layout.addWidget(self.failover_strategy, 1, 1)
        
        # 数据一致性检查
        strategy_layout.addWidget(QLabel("数据一致性检查:"), 2, 0)
        self.consistency_check = QCheckBox("启用跨源数据验证")
        self.consistency_check.setChecked(True)
        strategy_layout.addWidget(self.consistency_check, 2, 1)
        
        layout.addWidget(strategy_group)
        
        # 按资产类型配置路由优先级
        priority_group = QGroupBox("资产类型路由优先级")
        priority_layout = QVBoxLayout(priority_group)
        
        # 资产类型选择
        asset_select_layout = QHBoxLayout()
        asset_select_layout.addWidget(QLabel("资产类型:"))
        
        self.routing_asset_type = QComboBox()
        self.routing_asset_type.addItems([
            "股票", "期货", "数字货币", "外汇", "债券"
        ])
        self.routing_asset_type.currentTextChanged.connect(self.load_routing_priority)
        asset_select_layout.addWidget(self.routing_asset_type)
        
        asset_select_layout.addStretch()
        priority_layout.addLayout(asset_select_layout)
        
        # 优先级列表
        self.priority_table = QTableWidget()
        self.priority_table.setColumnCount(4)
        self.priority_table.setHorizontalHeaderLabels([
            "优先级", "插件名称", "状态", "操作"
        ])
        priority_layout.addWidget(self.priority_table)
        
        # 优先级调整按钮
        priority_btn_layout = QHBoxLayout()
        
        up_btn = QPushButton("⬆️ 上移")
        up_btn.clicked.connect(self.move_priority_up)
        priority_btn_layout.addWidget(up_btn)
        
        down_btn = QPushButton("⬇️ 下移")
        down_btn.clicked.connect(self.move_priority_down)
        priority_btn_layout.addWidget(down_btn)
        
        priority_btn_layout.addStretch()
        
        save_priority_btn = QPushButton("💾 保存优先级")
        save_priority_btn.clicked.connect(self.save_routing_priority)
        priority_btn_layout.addWidget(save_priority_btn)
        
        priority_layout.addLayout(priority_btn_layout)
        layout.addWidget(priority_group)
        
        return widget
    
    def _create_monitoring_tab(self) -> QWidget:
        """创建性能监控选项卡"""
        widget = QWidget()
        layout = QVBoxLayout(widget)
        
        # 实时状态概览
        status_group = QGroupBox("实时状态概览")
        status_layout = QGridLayout(status_group)
        
        # 状态指标
        self.total_requests_label = QLabel("0")
        self.success_rate_label = QLabel("0%")
        self.avg_response_time_label = QLabel("0ms")
        self.cache_hit_rate_label = QLabel("0%")
        
        status_layout.addWidget(QLabel("总请求数:"), 0, 0)
        status_layout.addWidget(self.total_requests_label, 0, 1)
        status_layout.addWidget(QLabel("成功率:"), 0, 2)
        status_layout.addWidget(self.success_rate_label, 0, 3)
        
        status_layout.addWidget(QLabel("平均响应时间:"), 1, 0)
        status_layout.addWidget(self.avg_response_time_label, 1, 1)
        status_layout.addWidget(QLabel("缓存命中率:"), 1, 2)
        status_layout.addWidget(self.cache_hit_rate_label, 1, 3)
        
        layout.addWidget(status_group)
        
        # 插件性能详情
        performance_group = QGroupBox("插件性能详情")
        performance_layout = QVBoxLayout(performance_group)
        
        self.performance_table = QTableWidget()
        self.performance_table.setColumnCount(7)
        self.performance_table.setHorizontalHeaderLabels([
            "插件名称", "请求数", "成功率", "平均响应时间", "错误次数", "最后错误", "操作"
        ])
        performance_layout.addWidget(self.performance_table)
        
        # 刷新按钮
        refresh_layout = QHBoxLayout()
        auto_refresh_cb = QCheckBox("自动刷新(5秒)")
        auto_refresh_cb.setChecked(True)
        auto_refresh_cb.stateChanged.connect(self.toggle_auto_refresh)
        refresh_layout.addWidget(auto_refresh_cb)
        
        refresh_layout.addStretch()
        
        manual_refresh_btn = QPushButton("🔄 立即刷新")
        manual_refresh_btn.clicked.connect(self.refresh_performance_data)
        refresh_layout.addWidget(manual_refresh_btn)
        
        performance_layout.addLayout(refresh_layout)
        layout.addWidget(performance_group)
        
        return widget
    
    def _create_quality_tab(self) -> QWidget:
        """创建数据质量选项卡"""
        widget = QWidget()
        layout = QVBoxLayout(widget)
        
        # 数据质量概览
        quality_group = QGroupBox("数据质量概览")
        quality_layout = QGridLayout(quality_group)
        
        # 质量指标
        self.completeness_score = QLabel("0%")
        self.accuracy_score = QLabel("0%")
        self.timeliness_score = QLabel("0%")
        self.consistency_score = QLabel("0%")
        
        quality_layout.addWidget(QLabel("完整性评分:"), 0, 0)
        quality_layout.addWidget(self.completeness_score, 0, 1)
        quality_layout.addWidget(QLabel("准确性评分:"), 0, 2)
        quality_layout.addWidget(self.accuracy_score, 0, 3)
        
        quality_layout.addWidget(QLabel("及时性评分:"), 1, 0)
        quality_layout.addWidget(self.timeliness_score, 1, 1)
        quality_layout.addWidget(QLabel("一致性评分:"), 1, 2)
        quality_layout.addWidget(self.consistency_score, 1, 3)
        
        layout.addWidget(quality_group)
        
        # 数据源质量详情
        source_quality_group = QGroupBox("数据源质量详情")
        source_quality_layout = QVBoxLayout(source_quality_group)
        
        self.quality_table = QTableWidget()
        self.quality_table.setColumnCount(6)
        self.quality_table.setHorizontalHeaderLabels([
            "插件名称", "完整性", "准确性", "及时性", "一致性", "综合评分"
        ])
        source_quality_layout.addWidget(self.quality_table)
        
        layout.addWidget(source_quality_group)
        
        # 质量告警设置
        alert_group = QGroupBox("质量告警设置")
        alert_layout = QGridLayout(alert_group)
        
        alert_layout.addWidget(QLabel("质量阈值:"), 0, 0)
        self.quality_threshold = QDoubleSpinBox()
        self.quality_threshold.setRange(0.0, 1.0)
        self.quality_threshold.setSingleStep(0.1)
        self.quality_threshold.setValue(0.8)
        self.quality_threshold.setSuffix(" (80%)")
        alert_layout.addWidget(self.quality_threshold, 0, 1)
        
        alert_layout.addWidget(QLabel("告警方式:"), 1, 0)
        self.alert_method = QComboBox()
        self.alert_method.addItems(["界面通知", "邮件通知", "短信通知", "全部"])
        alert_layout.addWidget(self.alert_method, 1, 1)
        
        layout.addWidget(alert_group)
        
        return widget


class DataSourcePluginConfigWidget(QWidget):
    """数据源插件配置小部件"""
    
    configChanged = pyqtSignal(str, dict)  # 配置变更信号
    
    def __init__(self, plugin_id: str, plugin_config: Dict[str, Any], parent=None):
        super().__init__(parent)
        self.plugin_id = plugin_id
        self.config = plugin_config.copy()
        self.config_widgets = {}
        
        self.init_ui()
        self.load_config()
    
    def init_ui(self):
        """初始化UI"""
        layout = QVBoxLayout(self)
        
        # 基础配置组
        basic_group = QGroupBox("基础配置")
        basic_layout = QFormLayout(basic_group)
        
        # 启用/禁用
        self.enabled_cb = QCheckBox()
        self.enabled_cb.setChecked(self.config.get('enabled', True))
        self.enabled_cb.stateChanged.connect(self.on_config_changed)
        basic_layout.addRow("启用插件:", self.enabled_cb)
        self.config_widgets['enabled'] = self.enabled_cb
        
        # 优先级
        self.priority_spin = QSpinBox()
        self.priority_spin.setRange(1, 100)
        self.priority_spin.setValue(self.config.get('priority', 50))
        self.priority_spin.valueChanged.connect(self.on_config_changed)
        basic_layout.addRow("优先级:", self.priority_spin)
        self.config_widgets['priority'] = self.priority_spin
        
        # 超时时间
        self.timeout_spin = QSpinBox()
        self.timeout_spin.setRange(1, 300)
        self.timeout_spin.setSuffix(" 秒")
        self.timeout_spin.setValue(self.config.get('timeout', 30))
        self.timeout_spin.valueChanged.connect(self.on_config_changed)
        basic_layout.addRow("超时时间:", self.timeout_spin)
        self.config_widgets['timeout'] = self.timeout_spin
        
        # 重试次数
        self.retry_spin = QSpinBox()
        self.retry_spin.setRange(0, 10)
        self.retry_spin.setValue(self.config.get('retry_count', 3))
        self.retry_spin.valueChanged.connect(self.on_config_changed)
        basic_layout.addRow("重试次数:", self.retry_spin)
        self.config_widgets['retry_count'] = self.retry_spin
        
        layout.addWidget(basic_group)
        
        # 缓存配置组
        cache_group = QGroupBox("缓存配置")
        cache_layout = QFormLayout(cache_group)
        
        # 缓存TTL
        self.cache_ttl_spin = QSpinBox()
        self.cache_ttl_spin.setRange(0, 3600)
        self.cache_ttl_spin.setSuffix(" 秒")
        self.cache_ttl_spin.setValue(self.config.get('cache_ttl', 300))
        self.cache_ttl_spin.valueChanged.connect(self.on_config_changed)
        cache_layout.addRow("缓存TTL:", self.cache_ttl_spin)
        self.config_widgets['cache_ttl'] = self.cache_ttl_spin
        
        # 启用缓存
        self.cache_enabled_cb = QCheckBox()
        self.cache_enabled_cb.setChecked(self.config.get('cache_enabled', True))
        self.cache_enabled_cb.stateChanged.connect(self.on_config_changed)
        cache_layout.addRow("启用缓存:", self.cache_enabled_cb)
        self.config_widgets['cache_enabled'] = self.cache_enabled_cb
        
        layout.addWidget(cache_group)
        
        # 限流配置组
        rate_limit_group = QGroupBox("限流配置")
        rate_limit_layout = QFormLayout(rate_limit_group)
        
        # 请求频率限制
        self.rate_limit_spin = QSpinBox()
        self.rate_limit_spin.setRange(1, 1000)
        self.rate_limit_spin.setSuffix(" 次/分钟")
        self.rate_limit_spin.setValue(self.config.get('rate_limit', 100))
        self.rate_limit_spin.valueChanged.connect(self.on_config_changed)
        rate_limit_layout.addRow("请求频率:", self.rate_limit_spin)
        self.config_widgets['rate_limit'] = self.rate_limit_spin
        
        layout.addWidget(rate_limit_group)
        
        # 高级配置组（插件特定配置）
        self.advanced_group = QGroupBox("高级配置")
        self.advanced_layout = QFormLayout(self.advanced_group)
        layout.addWidget(self.advanced_group)
        
        # 底部按钮
        button_layout = QHBoxLayout()
        
        # 测试连接按钮
        test_btn = QPushButton("🔍 测试连接")
        test_btn.clicked.connect(self.test_connection)
        button_layout.addWidget(test_btn)
        
        # 重置按钮
        reset_btn = QPushButton("🔄 重置")
        reset_btn.clicked.connect(self.reset_config)
        button_layout.addWidget(reset_btn)
        
        button_layout.addStretch()
        
        # 保存按钮
        save_btn = QPushButton("💾 保存")
        save_btn.clicked.connect(self.save_config)
        button_layout.addWidget(save_btn)
        
        layout.addLayout(button_layout)
    
    def load_config(self):
        """加载配置"""
        # 加载插件特定的高级配置
        advanced_config = self.config.get('advanced_config', {})
        
        # 根据插件类型动态创建配置控件
        plugin_type = self.config.get('plugin_type', 'unknown')
        
        if plugin_type == 'akshare':
            self._create_akshare_config()
        elif plugin_type == 'eastmoney':
            self._create_eastmoney_config()
        elif plugin_type == 'binance':
            self._create_binance_config()
        # ... 其他插件类型
    
    def _create_akshare_config(self):
        """创建AKShare插件特定配置"""
        # 数据源选择
        data_source_combo = QComboBox()
        data_source_combo.addItems(["新浪", "东财", "腾讯", "网易"])
        data_source_combo.setCurrentText(self.config.get('data_source', '新浪'))
        data_source_combo.currentTextChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("数据源:", data_source_combo)
        self.config_widgets['data_source'] = data_source_combo
        
        # 调整复权
        adjust_combo = QComboBox()
        adjust_combo.addItems(["qfq", "hfq", ""])
        adjust_combo.setCurrentText(self.config.get('adjust', 'qfq'))
        adjust_combo.currentTextChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("复权方式:", adjust_combo)
        self.config_widgets['adjust'] = adjust_combo
    
    def _create_eastmoney_config(self):
        """创建东方财富插件特定配置"""
        # API密钥
        api_key_edit = QLineEdit()
        api_key_edit.setText(self.config.get('api_key', ''))
        api_key_edit.setEchoMode(QLineEdit.Password)
        api_key_edit.textChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("API密钥:", api_key_edit)
        self.config_widgets['api_key'] = api_key_edit
        
        # 代理设置
        proxy_edit = QLineEdit()
        proxy_edit.setText(self.config.get('proxy', ''))
        proxy_edit.setPlaceholderText("http://proxy.example.com:8080")
        proxy_edit.textChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("代理地址:", proxy_edit)
        self.config_widgets['proxy'] = proxy_edit
    
    def _create_binance_config(self):
        """创建Binance插件特定配置"""
        # API密钥
        api_key_edit = QLineEdit()
        api_key_edit.setText(self.config.get('api_key', ''))
        api_key_edit.textChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("API Key:", api_key_edit)
        self.config_widgets['api_key'] = api_key_edit
        
        # 密钥
        secret_edit = QLineEdit()
        secret_edit.setText(self.config.get('secret', ''))
        secret_edit.setEchoMode(QLineEdit.Password)
        secret_edit.textChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("Secret Key:", secret_edit)
        self.config_widgets['secret'] = secret_edit
        
        # 测试网络
        testnet_cb = QCheckBox()
        testnet_cb.setChecked(self.config.get('testnet', False))
        testnet_cb.stateChanged.connect(self.on_config_changed)
        self.advanced_layout.addRow("测试网络:", testnet_cb)
        self.config_widgets['testnet'] = testnet_cb
    
    def on_config_changed(self):
        """配置变更处理"""
        # 收集当前配置
        current_config = {}
        
        for key, widget in self.config_widgets.items():
            if isinstance(widget, QCheckBox):
                current_config[key] = widget.isChecked()
            elif isinstance(widget, (QSpinBox, QDoubleSpinBox)):
                current_config[key] = widget.value()
            elif isinstance(widget, QComboBox):
                current_config[key] = widget.currentText()
            elif isinstance(widget, QLineEdit):
                current_config[key] = widget.text()
        
        # 更新配置
        self.config.update(current_config)
        
        # 发送配置变更信号
        self.configChanged.emit(self.plugin_id, self.config)
    
    def test_connection(self):
        """测试连接"""
        try:
            # 这里调用插件的测试连接方法
            QMessageBox.information(self, "测试连接", f"插件 {self.plugin_id} 连接测试成功！")
        except Exception as e:
            QMessageBox.warning(self, "连接失败", f"插件 {self.plugin_id} 连接测试失败：\n{str(e)}")
    
    def reset_config(self):
        """重置配置"""
        reply = QMessageBox.question(
            self, "确认重置", 
            "确定要重置所有配置为默认值吗？",
            QMessageBox.Yes | QMessageBox.No
        )
        
        if reply == QMessageBox.Yes:
            # 重置为默认配置
            self.config = self._get_default_config()
            self.load_config()
            self.on_config_changed()
    
    def save_config(self):
        """保存配置"""
        try:
            # 保存配置到文件
            config_manager = self.parent().config_manager
            if config_manager.update_plugin_config(self.plugin_id, self.config):
                QMessageBox.information(self, "保存成功", "插件配置已保存！")
            else:
                QMessageBox.warning(self, "保存失败", "插件配置保存失败！")
        except Exception as e:
            QMessageBox.critical(self, "保存错误", f"保存配置时发生错误：\n{str(e)}")
    
    def _get_default_config(self) -> Dict[str, Any]:
        """获取默认配置"""
        return {
            'enabled': True,
            'priority': 50,
            'timeout': 30,
            'retry_count': 3,
            'cache_ttl': 300,
            'cache_enabled': True,
            'rate_limit': 100
        }
```

---

## 🔗 集成到现有菜单系统

### 1. 扩展主菜单栏

```python
# gui/menu_bar.py - 在现有菜单中添加数据源管理
class MainMenuBar(QMenuBar):
    def __init__(self, parent=None):
        # ... 现有代码 ...
        
        # 扩展工具菜单
        tools_menu = self.addMenu("工具(&T)")
        
        # 现有菜单项...
        
        # 新增：数据源管理菜单项
        data_source_action = QAction("🔌 数据源管理", self)
        data_source_action.setShortcut("Ctrl+Shift+D")
        data_source_action.setStatusTip("管理数据源插件配置")
        data_source_action.triggered.connect(self.open_data_source_manager)
        tools_menu.addAction(data_source_action)
    
    def open_data_source_manager(self):
        """打开数据源管理器"""
        from gui.dialogs.data_source_plugin_manager_dialog import DataSourcePluginManagerDialog
        
        dialog = DataSourcePluginManagerDialog(self.plugin_manager, self)
        dialog.exec_()
```

### 2. 在现有插件管理器中集成

```python
# gui/dialogs/plugin_manager_dialog.py - 扩展现有插件管理器
class PluginManagerDialog(QDialog):
    def __init__(self, plugin_manager: PluginManager, parent=None):
        # ... 现有初始化代码 ...
        
        # 在现有选项卡中添加数据源选项卡
        self.data_source_tab = self._create_data_source_tab()
        self.tab_widget.addTab(self.data_source_tab, "数据源插件")
    
    def _create_data_source_tab(self) -> QWidget:
        """创建数据源插件选项卡"""
        # 直接嵌入DataSourcePluginManagerDialog的内容
        from gui.dialogs.data_source_plugin_manager_dialog import DataSourcePluginManagerDialog
        
        # 创建嵌入式数据源管理器
        data_source_manager = DataSourcePluginManagerDialog(self.plugin_manager)
        
        # 移除对话框装饰，作为选项卡内容
        data_source_manager.setWindowFlags(Qt.Widget)
        
        return data_source_manager
```

---

## 🎛️ 动态参数修改功能

### 1. 实时配置更新机制

```python
# core/plugin_config_manager.py - 增强配置管理器
class PluginConfigManager:
    def __init__(self):
        # ... 现有代码 ...
        
        # 新增：配置变更监听器
        self.config_change_listeners = {}
        self.config_file_watchers = {}
    
    def add_config_change_listener(self, plugin_id: str, callback: Callable):
        """添加配置变更监听器"""
        if plugin_id not in self.config_change_listeners:
            self.config_change_listeners[plugin_id] = []
        
        self.config_change_listeners[plugin_id].append(callback)
    
    def notify_config_change(self, plugin_id: str, new_config: Dict[str, Any]):
        """通知配置变更"""
        # 保存配置
        self.update_plugin_config(plugin_id, new_config)
        
        # 通知所有监听器
        if plugin_id in self.config_change_listeners:
            for callback in self.config_change_listeners[plugin_id]:
                try:
                    callback(plugin_id, new_config)
                except Exception as e:
                    logger.error(f"配置变更通知失败 {plugin_id}: {e}")
    
    def hot_reload_plugin_config(self, plugin_id: str) -> bool:
        """热重载插件配置"""
        try:
            # 从文件重新加载配置
            config_file = self.config_dir / f"{plugin_id}.yaml"
            if config_file.exists():
                with open(config_file, 'r', encoding='utf-8') as f:
                    new_config = yaml.safe_load(f)
                
                # 更新内存中的配置
                if plugin_id in self.plugin_configs:
                    self.plugin_configs[plugin_id].config_data.update(new_config)
                
                # 通知配置变更
                self.notify_config_change(plugin_id, new_config)
                
                logger.info(f"插件配置热重载成功: {plugin_id}")
                return True
            
        except Exception as e:
            logger.error(f"插件配置热重载失败 {plugin_id}: {e}")
            return False
```

### 2. 插件参数实时生效

```python
# 数据源插件基类 - 支持配置热更新
class BaseDataSourcePlugin(IDataSourcePlugin):
    def __init__(self, plugin_id: str, config_manager: PluginConfigManager):
        self.plugin_id = plugin_id
        self.config_manager = config_manager
        self.current_config = {}
        
        # 注册配置变更监听器
        config_manager.add_config_change_listener(
            plugin_id, self.on_config_changed
        )
    
    def on_config_changed(self, plugin_id: str, new_config: Dict[str, Any]):
        """配置变更处理"""
        if plugin_id != self.plugin_id:
            return
        
        # 更新配置
        old_config = self.current_config.copy()
        self.current_config.update(new_config)
        
        # 应用配置变更
        self._apply_config_changes(old_config, new_config)
        
        logger.info(f"插件 {plugin_id} 配置已更新")
    
    def _apply_config_changes(self, old_config: Dict[str, Any], 
                            new_config: Dict[str, Any]):
        """应用配置变更"""
        # 检查需要重新初始化的配置项
        critical_changes = [
            'api_key', 'secret', 'base_url', 'proxy'
        ]
        
        for key in critical_changes:
            if old_config.get(key) != new_config.get(key):
                logger.info(f"检测到关键配置变更: {key}")
                self._reinitialize_connection()
                break
        
        # 应用非关键配置变更
        if old_config.get('timeout') != new_config.get('timeout'):
            self.timeout = new_config.get('timeout', 30)
        
        if old_config.get('retry_count') != new_config.get('retry_count'):
            self.retry_count = new_config.get('retry_count', 3)
        
        if old_config.get('rate_limit') != new_config.get('rate_limit'):
            self.rate_limit = new_config.get('rate_limit', 100)
            self._update_rate_limiter()
    
    def _reinitialize_connection(self):
        """重新初始化连接"""
        try:
            # 关闭现有连接
            self.disconnect()
            
            # 使用新配置重新连接
            self.initialize(self.current_config)
            
            logger.info(f"插件 {self.plugin_id} 连接重新初始化成功")
            
        except Exception as e:
            logger.error(f"插件 {self.plugin_id} 连接重新初始化失败: {e}")
```

---

## 📊 配置管理界面集成效果

### 1. 统一的插件管理入口

用户可以通过以下方式访问数据源插件管理：

1. **主菜单**: 工具 → 数据源管理 (Ctrl+Shift+D)
2. **现有插件管理器**: 插件管理对话框 → 数据源插件选项卡
3. **快捷方式**: 系统托盘右键菜单

### 2. 丰富的配置控制界面

**基础配置**:
- ✅ 插件启用/禁用开关
- ✅ 优先级数值调整 (1-100)
- ✅ 超时时间设置 (1-300秒)
- ✅ 重试次数设置 (0-10次)

**缓存配置**:
- ✅ 缓存TTL设置 (0-3600秒)
- ✅ 缓存启用/禁用开关

**限流配置**:
- ✅ 请求频率限制 (1-1000次/分钟)

**高级配置** (根据插件类型动态生成):
- ✅ AKShare: 数据源选择、复权方式
- ✅ 东方财富: API密钥、代理设置
- ✅ Binance: API Key、Secret Key、测试网络

### 3. 实时生效机制

**配置修改后立即生效**:
- ✅ 超时时间调整立即应用
- ✅ 重试次数修改实时更新
- ✅ 缓存TTL变更即时生效
- ✅ API密钥更新后自动重连

**热重载支持**:
- ✅ 无需重启应用程序
- ✅ 配置变更自动保存
- ✅ 插件状态实时同步

---

## ✅ 集成验证清单

### 功能完整性验证

- [ ] 数据源插件在现有插件管理器中正确显示
- [ ] 插件配置界面支持所有必要的参数类型
- [ ] 配置修改能够实时生效，无需重启
- [ ] 插件启用/禁用状态正确同步
- [ ] 路由优先级调整功能正常工作

### 界面集成验证

- [ ] 数据源管理菜单项正确添加到主菜单
- [ ] 插件管理对话框正确显示数据源选项卡
- [ ] 配置界面样式与现有UI保持一致
- [ ] 所有按钮和控件响应正常
- [ ] 错误提示和成功消息正确显示

### 性能影响验证

- [ ] 插件配置加载时间 < 500ms
- [ ] 配置修改响应时间 < 100ms
- [ ] 内存使用增长 < 50MB
- [ ] 不影响现有插件管理功能性能

---

## 🎯 总结

通过扩展现有的插件管理系统，新的插件化数据源可以完美集成到FactorWeave-Quant 中，实现：

### ✅ 完全兼容
- 基于现有PluginConfigManager和PluginManager
- 复用现有的配置管理UI框架
- 保持与现有插件类型的一致性

### ✅ 功能完整
- 支持所有必要的插件参数配置
- 提供丰富的管理界面（插件管理、路由配置、性能监控、数据质量）
- 支持配置的实时修改和热重载

### ✅ 用户友好
- 统一的管理入口和操作体验
- 直观的可视化配置界面
- 实时的状态反馈和错误提示

### ✅ 扩展性强
- 支持新插件类型的动态添加
- 支持插件特定配置的自定义界面
- 支持未来功能的渐进式增强

这个集成方案确保了新的插件化数据源系统能够无缝融入现有的FactorWeave-Quant 架构，为用户提供强大而易用的数据源管理功能。 